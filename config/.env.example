# ⚠️  DEPRECATED — this file is kept for historical reference only.
# The canonical environment configuration is at the repository root:
#   .env.example   (template)
#   .env.local     (machine-local, never committed)
# Do NOT use this file for new setups.
