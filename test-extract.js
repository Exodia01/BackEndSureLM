(async function() { const path = require('path'); const fs = require('fs'); try { const { PDFParse } = require('pdf-parse'); const pdfPath = path.join(__dirname, 'test', 'Kotak-e-Term-Plan-Brochure.pdf'); const buffer = fs.readFileSync(pdfPath); const parser = new PDFParse(); const data = await parser.parse(buffer); console.log('Num pages:', data.numpages); process.exit(0); } catch(e) { console.error('Error:', e.message); process.exit(1); } })();
