<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode -->
    <style name="Theme.SureLM" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/surelm_background</item>
        <item name="android:colorBackground">@color/surelm_background</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.SureLM.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/purple_700</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Dialog theme -->
    <style name="Theme.SureLM.Dialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:background">@color/white</item>
    </style>
</resources>
